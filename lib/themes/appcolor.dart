import 'dart:ui';

import 'package:flutter/material.dart';

class AppColor {
  static const indigo = Colors.indigo;
  static const primary = Color.fromRGBO(49, 87, 110, 1.0);
  static const white = Color.fromRGBO(255, 255, 255, 1.0);
  static const dark = Color.fromRGBO(0, 0, 0, 1.0);
}
